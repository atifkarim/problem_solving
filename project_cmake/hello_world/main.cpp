/*
source : https://riptutorial.com/Download/cmake.pdf
this command used to make and run the code: cmake . && cmake --build . && ./app

Initially there was only main.cpp and CmakeLists.txt file will present. Then all other files andf folders will
come after the build
*/

#include <iostream>
int main()
{
 std::cout << "Hello World!\n";
 return 0;
}