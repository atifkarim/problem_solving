#include <stdio.h>
#include <string.h>

int main(){

    // char *name = "Atif";
    char name[30];
    printf("\nPlease enter your string val: ");
    gets(name);
    printf("\nActual string is: %s", name);
    // strrev(name);
    printf("\nActual string is: %s", name);


    printf("\n");

    return 0;

}